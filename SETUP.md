# 🔧 คู่มือการตั้งค่าระบบสำหรับผู้ดูแล

## ขั้นตอนการตั้งค่า API Key สำหรับผู้ใช้ทั้งหมด

### วิธีที่ 1: Hardcode API Key (แนะนำสำหรับ GitHub Pages) ⭐

#### ขั้นตอนที่ 1: รับ API Key ฟรี

1. เปิด [Google AI Studio](https://aistudio.google.com/app/apikey)
2. เข้าสู่ระบบด้วย Google Account
3. คลิก **"Create API Key"**
4. คัดลอก API Key ที่ได้

#### ขั้นตอนที่ 2: ใส่ API Key ในโค้ด

1. เปิดไฟล์ `index.html`
2. ค้นหาบรรทัดที่มีข้อความ:
   ```javascript
   const SYSTEM_API_KEY = "YOUR_GEMINI_API_KEY_HERE";
   ```

3. เปลี่ยนเป็น:
   ```javascript
   const SYSTEM_API_KEY = "AIzaSyB..."; // API Key ของคุณ
   ```

#### ขั้นตอนที่ 3: Commit และ Push

```bash
git add index.html
git commit -m "Update system API key"
git push origin main
```

#### ขั้นตอนที่ 4: รอ GitHub Pages Deploy

- รอประมาณ 1-2 นาที
- เข้าไปที่ https://koki-assawin.github.io/AI-Powered-C/
- ทดสอบการใช้งาน

---

## ⚠️ ข้อควรระวัง

### ข้อดี ✅
- ✅ ผู้ใช้ไม่ต้องตั้งค่าอะไรเลย
- ✅ ใช้งานได้ทันทีเมื่อเปิดเว็บ
- ✅ ไม่ต้องมี Backend Server
- ✅ ฟรี 100%

### ข้อเสีย ⚠️
- ⚠️ API Key จะเห็นได้ในโค้ด (ถ้าเปิด DevTools)
- ⚠️ คนอื่นสามารถนำ API Key ไปใช้ได้
- ⚠️ อาจเกิน Rate Limit ถ้าใช้งานเยอะ

### วิธีป้องกัน 🛡️

1. **ตั้ง Quota Limit** ใน Google Cloud Console
2. **ติดตามการใช้งาน** เป็นประจำ
3. **เตรียม API Key สำรอง** กรณีเกิน quota
4. **แจ้งผู้ใช้** ให้ใช้อย่างประหยัด

---

## วิธีที่ 2: ใช้ Firebase Realtime Database (ปลอดภัยกว่า) 🔐

### ข้อดี
- 🔒 API Key ไม่ปรากฏในโค้ด
- 🔄 เปลี่ยน API Key ได้โดยไม่ต้อง redeploy
- 📊 ควบคุมการเข้าถึงได้

### ขั้นตอน

#### 1. สร้าง Firebase Project

1. ไปที่ [Firebase Console](https://console.firebase.google.com/)
2. สร้าง Project ใหม่
3. เลือก Realtime Database
4. ตั้งค่า Rules:
   ```json
   {
     "rules": {
       "config": {
         ".read": true,
         ".write": false
       }
     }
   }
   ```

#### 2. เพิ่ม API Key ใน Database

ใน Realtime Database ให้สร้าง structure:
```json
{
  "config": {
    "gemini_api_key": "YOUR_API_KEY_HERE"
  }
}
```

#### 3. แก้ไขโค้ดใน index.html

เพิ่มโค้ดนี้ใน `useEffect`:

```javascript
useEffect(() => {
    // ดึง API Key จาก Firebase
    fetch('https://YOUR_PROJECT.firebaseio.com/config/gemini_api_key.json')
        .then(res => res.json())
        .then(apiKey => {
            if (apiKey) {
                setApiKey(apiKey);
            }
        })
        .catch(err => {
            console.error('Error loading API key:', err);
        });
}, []);
```

---

## 📊 เปรียบเทียบ 2 วิธี

| Feature | Hardcode | Firebase |
|---------|----------|----------|
| **ความง่าย** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **ความปลอดภัย** | ⭐⭐ | ⭐⭐⭐⭐ |
| **ค่าใช้จ่าย** | ฟรี | ฟรี (Spark plan) |
| **แก้ไข API Key** | ต้อง redeploy | แก้ใน DB ทันที |
| **Setup Time** | 5 นาที | 15 นาที |

---

## 🎯 คำแนะนำ

### สำหรับการใช้งานในโรงเรียน/มหาวิทยาลัย
👉 **ใช้วิธีที่ 1 (Hardcode)**
- เพราะง่าย รวดเร็ว
- ควบคุมผู้ใช้ได้
- สอนวิธีการใช้งานอย่างประหยัด

### สำหรับการใช้งานสาธารณะ
👉 **ใช้วิธีที่ 2 (Firebase)**
- ปลอดภัยกว่า
- เปลี่ยน API Key ได้ง่าย
- ควบคุมการเข้าถึงได้ดีกว่า

---

## 🆘 แก้ปัญหา

### ปัญหา: API Key ใช้งานไม่ได้

**วิธีแก้:**
1. ตรวจสอบว่า API Key ถูกต้อง
2. เช็คว่าไม่มีช่องว่างหน้าหลัง
3. ทดสอบ API Key ที่ [Google AI Studio](https://aistudio.google.com/)

### ปัญหา: เกิน Rate Limit

**วิธีแก้:**
1. ตรวจสอบการใช้งานที่ [Google Cloud Console](https://console.cloud.google.com/)
2. เพิ่ม API Key สำรอง
3. ตั้ง Rate Limiting ในโค้ด

### ปัญหา: ผู้ใช้แจ้งว่าระบบไม่ทำงาน

**ตรวจสอบ:**
1. เปิด DevTools (F12) ดู Console มี error หรือไม่
2. ตรวจสอบว่า API Key ยังใช้งานได้
3. ตรวจสอบ Network tab ว่า API call สำเร็จหรือไม่

---

## 📞 ติดต่อ

หากมีปัญหาหรือข้อสงสัย:
- Email: koki.assawin@gmail.com
- GitHub Issues: https://github.com/koki-assawin/AI-Powered-C/issues

---

© 2025 AI-Powered Code Practice System | Triam Udom Suksa of The South
