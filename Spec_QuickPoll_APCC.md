# สเปกโมดูล Quick Poll สำหรับระบบ AI-Powered Coding Coach (APCC)

เอกสารข้อกำหนดทางเทคนิคสำหรับพัฒนาโมดูลสำรวจความเห็นทันทีและการโหวตสองรอบ  
ผู้จัดทำ นายอัศวิน จุลมูล | เวอร์ชัน 1.0 | สถานะ ร่างเพื่อพัฒนา

---

## 1. ที่มาและเหตุผล

แผนการจัดการเรียนรู้ที่ 19 ใช้กิจกรรมสองอย่างที่ต้องการเครื่องมือรับคำตอบจากผู้เรียนทั้งชั้นพร้อมกัน

**ขั้น E1** Think–Pair–Share ให้ผู้เรียน 32 คนพิมพ์คำตอบปลายเปิดส่งเข้าระบบภายใน 1.5 นาที แล้วครูฉายผลรวมเป็นคลังคำทันที เพื่อให้ผู้เรียนได้แสดงความคิดครบทุกคน แทนที่จะมีเพียง 1 ถึง 2 คนตอบด้วยวาจา

**ขั้น E2** การโหวตสองรอบตามเทคนิค Peer Instruction ผู้เรียนโหวตคำตอบครั้งแรกโดยไม่ปรึกษาใคร จากนั้นถกเถียงกับเพื่อน 3.5 นาที แล้วโหวตซ้ำ ครูฉายกราฟเปรียบเทียบผลสองรอบให้เห็นว่าความเข้าใจเปลี่ยนไปอย่างไร

ปัจจุบันต้องพึ่งเครื่องมือภายนอก ซึ่งมีข้อจำกัดสามประการ คือ แผนใช้ 7 คำถามต่อคาบซึ่งเกินโควตาของแผนฟรี ข้อมูลอยู่นอกระบบจึงนำมาเป็นหลักฐานเชิงประจักษ์ได้ยาก และผู้เรียนต้องสลับแอปพลิเคชันระหว่างคาบซึ่งเสียเวลาและเสียสมาธิ

**คุณค่าที่แท้จริงของการพัฒนาเอง** มิใช่การประหยัดค่าสมัครสมาชิก แต่คือการที่ข้อมูลการโหวตถูกบันทึกลงฐานข้อมูลเดียวกับข้อมูลการส่งงาน ทำให้คำนวณตัวชี้วัดที่เครื่องมือภายนอกให้ไม่ได้ นั่นคือ **อัตราการเปลี่ยนคำตอบจากผิดเป็นถูกหลังการถกเถียงกับเพื่อน** ซึ่งเป็นหลักฐานโดยตรงว่าการเรียนรู้จากเพื่อนเกิดขึ้นจริง และเชื่อมกับเครื่องมือวิจัยฉบับที่ 8 แบบบันทึกข้อมูลเชิงประจักษ์จากระบบได้ทันที

---

## 2. ขอบเขตของโมดูล

### อยู่ในขอบเขต
- คำถามแบบเลือกตอบ รองรับการถามซ้ำสองรอบและเปรียบเทียบผล
- คำถามแบบปลายเปิดข้อความสั้น พร้อมแสดงผลเป็นคลังคำ
- หน้าจอควบคุมสำหรับครูขณะสอน และหน้าจอฉายผลสำหรับโปรเจคเตอร์
- การบันทึกข้อมูลรายบุคคลเพื่อการวิจัย และการส่งออกเป็นไฟล์ CSV
- การผูกคำถามเข้ากับหมายเลขแผนและขั้นของ 5Es

### ไม่อยู่ในขอบเขตของเวอร์ชันแรก
- คำถามแบบจัดอันดับ แบบจับคู่ หรือแบบเติมคำ
- การให้คะแนนอัตโนมัติเข้าสมุดคะแนน เพราะการโหวตเป็นการประเมินเพื่อพัฒนา ไม่คิดคะแนน
- การใช้งานข้ามโรงเรียนหรือระบบหลายผู้เช่า

---

## 3. ข้อจำกัดทางเทคนิคที่ต้องออกแบบให้สอดคล้อง

| ข้อจำกัด | ผลต่อการออกแบบ |
|---|---|
| โฮสต์บน GitHub Pages เป็นเว็บสถิต ไม่มีเซิร์ฟเวอร์ของตนเอง | ตรรกะทั้งหมดทำงานฝั่งไคลเอนต์ ใช้ Firestore เป็นทั้งฐานข้อมูลและช่องทางสื่อสารแบบเรียลไทม์ |
| ผู้เรียนใช้ซิมอินเทอร์เน็ต 30 กิกะไบต์ต่อเดือน | ต้องประหยัดปริมาณข้อมูล ผู้เรียนไม่ต้องรับผลรวมของทั้งห้อง รับเฉพาะสถานะคำถามที่เปลี่ยน |
| React 17 และ Firestore ที่ใช้อยู่เดิม | ใช้ onSnapshot สำหรับการอัปเดตสด ไม่ต้องเพิ่มไลบรารีสื่อสารใหม่ |
| Chart.js ที่ใช้อยู่เดิม | ใช้สร้างกราฟแท่งและกราฟแท่งคู่ ไม่ต้องเพิ่มไลบรารีกราฟใหม่ |
| ผู้เรียน 32 คนต่อห้อง | ปริมาณข้อมูลเล็กมาก ครูสามารถรับฟังการเปลี่ยนแปลงของเอกสารทั้ง 32 รายการพร้อมกันได้โดยไม่มีปัญหาด้านสมรรถนะ |
| เวลาที่ใช้ในคาบสั้นมาก | ผู้เรียนต้องเข้าถึงคำถามได้ภายใน 10 วินาที ห้ามมีขั้นตอนล็อกอินซ้ำ ให้ใช้บัญชี APCC เดิม |

---

## 4. แบบจำลองข้อมูล (Firestore)

### หลักการสำคัญ แยกเนื้อหาคำถามออกจากการใช้งานแต่ละครั้ง

เนื้อหาคำถามเป็นสิ่งที่ใช้ซ้ำได้ตลอดไป ส่วนสถานะการเปิดปิดและคำตอบของผู้เรียนเป็นของการสอนครั้งนั้นครั้งเดียว หากเก็บรวมกันไว้ในเอกสารเดียว เมื่อนำคำถามเดิมไปใช้กับผู้เรียนรุ่นถัดไป ข้อมูลเดิมจะถูกทับและสูญหาย จึงต้องแยกออกเป็นสามคอลเลกชัน

```
pollTemplates   เนื้อหาคำถาม ใช้ซ้ำได้ไม่จำกัด ไม่มีสถานะ
pollSessions    การนำคำถามไปใช้หนึ่งครั้งกับหนึ่งห้องเรียน มีสถานะและเวลา
  └─ responses  คำตอบของผู้เรียนในการใช้ครั้งนั้น
```

### 4.1 คอลเลกชัน pollTemplates (คลังคำถาม ใช้ซ้ำได้)

```
pollTemplates/{templateId}
{
  subject: "ว31281",               // รหัสรายวิชา รองรับการขยายไปวิชาอื่น
  planId: "19",                    // หมายเลขแผน ใช้กรองคำถามของแต่ละแผน
  unitId: "3",                     // หน่วยการเรียนรู้
  stage: "E2",                     // ขั้นของ 5Es
  seq: 1,                          // ลำดับภายในขั้น
  loCode: "LO7",                   // ผลการเรียนรู้ที่คำถามนี้วัด
  bloom: "Analyze",
  type: "choice",                  // "choice" หรือ "text"
  prompt: "ถ้ารันโค้ดชุดนี้ จะได้อะไร",
  media: {                         // ทุกฟิลด์เป็นตัวเลือก ใส่เฉพาะที่ใช้
    code: "for(i=1; i>0; i++) { printf(\"%d \", i); }",
    language: "c",                 // ใช้เลือกการเน้นสีของโค้ด
    imageUrl: null,                // รองรับวิชาที่ใช้ภาพเป็นสิ่งเร้า
    text: null                     // รองรับสถานการณ์ที่เป็นข้อความยาว
  },
  options: [
    { id: "a", text: "แสดง 1 2 3 4 5 แล้วจบ" },
    { id: "b", text: "แสดงเลข 1 ซ้ำไม่รู้จบ" },
    { id: "c", text: "ไม่แสดงอะไรเลย" },
    { id: "d", text: "คอมไพล์ไม่ผ่าน" }
  ],
  correctOptionId: "b",            // null ได้ ถ้าเป็นคำถามที่ไม่มีถูกผิด
  misconceptions: {                // ตัวเลือกลวงสื่อความเข้าใจคลาดเคลื่อนใด
    a: "เข้าใจว่า for เพิ่มค่าให้เองเหมือน i++",
    c: "เข้าใจว่าเงื่อนไขเป็นเท็จตั้งแต่รอบแรก",
    d: "สับสนระหว่างความผิดพลาดเชิงตรรกะกับไวยากรณ์"
  },
  defaultRounds: 2,
  defaultSeconds: { r1: 150, r2: 60 },
  tags: ["for-loop", "infinite-loop"],
  createdBy: "<teacher-uid>",
  createdAt: <timestamp>,
  version: 1
}
```

ฟิลด์ `misconceptions` มิใช่ของประดับ เมื่อครูฉายผลแล้วพบว่าผู้เรียนจำนวนมากเลือกตัวเลือก a ระบบจะแสดงให้ครูเห็นทันทีว่าความเข้าใจคลาดเคลื่อนคืออะไร ช่วยให้ครูอธิบายได้ตรงจุดโดยไม่ต้องเดา และเป็นข้อมูลที่นำไปเขียนในบันทึกหลังการสอนได้โดยตรง

### 4.2 คอลเลกชัน pollSessions (การใช้งานหนึ่งครั้ง)

```
pollSessions/{sessionId}
{
  templateId: "<templateId>",      // อ้างถึงคำถามในคลัง
  snapshot: { ... },               // สำเนาเนื้อหาคำถาม ณ เวลาที่ใช้
  classId: "m4-6-2568-1",
  teacherUid: "<uid>",
  taughtOn: <timestamp>,
  rounds: 2,
  status: "closed",                // draft | round1_open | round1_closed |
                                   // round2_open | round2_closed | revealed
  activeRound: 2,
  expectedN: 32,
  openedAt: { r1: <ts>, r2: <ts> },
  closedAt: { r1: <ts>, r2: <ts> }
}
```

**เหตุผลที่ต้องเก็บ snapshot** หากครูแก้ไขคำถามในคลังภายหลัง ข้อมูลวิจัยของการสอนครั้งก่อนต้องยังคงอ้างอิงข้อความที่ใช้จริงในวันนั้น มิฉะนั้นจะตรวจสอบย้อนกลับไม่ได้ว่าผู้เรียนตอบคำถามที่มีถ้อยคำอย่างไร ซึ่งเป็นข้อกำหนดพื้นฐานของความน่าเชื่อถือในงานวิจัย

### 4.3 คอลเลกชันย่อย responses

หนึ่งเอกสารต่อผู้เรียนหนึ่งคนต่อหนึ่งการใช้งาน ใช้รหัสผู้ใช้เป็นรหัสเอกสาร เพื่อป้องกันการส่งซ้ำโดยอัตโนมัติ

```
pollSessions/{sessionId}/responses/{studentUid}
{
  studentUid: "<uid>",
  displayNo: 17,                   // เลขที่ในห้อง ใช้แทนชื่อเมื่อส่งออกข้อมูล
  r1: { optionId: "a", text: null, at: <ts> },
  r2: { optionId: "b", text: null, at: <ts> },
  changed: true,
  transition: "wrong_to_right"     // right_to_right | wrong_to_right |
                                   // right_to_wrong | wrong_to_wrong
}
```

### 4.4 คอลเลกชัน pollAggregates

```
pollAggregates/{sessionId}
{
  templateId: "<templateId>", classId: "m4-6-2568-1", taughtOn: <ts>,
  n: 32, responded: { r1: 32, r2: 31 },
  countsR1: { a: 12, b: 9, c: 7, d: 4 },
  countsR2: { a: 3, b: 25, c: 2, d: 2 },
  correctRateR1: 0.281, correctRateR2: 0.781,
  transitions: { wrong_to_right: 17, right_to_right: 8,
                 right_to_wrong: 1, wrong_to_wrong: 5 },
  normalizedGain: 0.696
}
```

การแยกเช่นนี้ทำให้เปรียบเทียบผลของคำถามเดียวกันข้ามห้องเรียนและข้ามปีการศึกษาได้ ซึ่งเป็นหลักฐานการขยายผลที่มีน้ำหนักมากในการประเมินวิทยฐานะ เพราะแสดงว่านวัตกรรมถูกใช้ซ้ำอย่างเป็นระบบมิใช่ใช้ครั้งเดียว

### 4.5 สิ่งที่ต้องทำเพื่อให้ใช้กับวิชาอื่นได้

โครงสร้างข้างต้นออกแบบให้เป็นกลางต่อวิชาแล้ว โดยฟิลด์ `subject` `media.imageUrl` และ `media.text` รองรับคำถามที่ไม่ใช่โค้ด สิ่งที่ต้องระวังคือส่วนแสดงผลต้องตรวจสอบว่ามีฟิลด์ใดบ้างแล้วจึงเลือกวิธีแสดง ห้ามเขียนโค้ดที่สมมติว่าต้องมี `media.code` เสมอ

## 5. หน้าจอและกระแสการทำงาน

### 5.1 หน้าจอผู้เรียน

**สถานะรอ** แสดงข้อความว่า รอครูเปิดคำถาม พร้อมชื่อขั้นของบทเรียน ไม่มีปุ่มใดกดได้

**สถานะตอบได้** แสดงโจทย์ ชุดคำสั่งในกล่อง monospace และปุ่มตัวเลือกขนาดใหญ่สี่ปุ่มเรียงแนวตั้ง แต่ละปุ่มสูงไม่น้อยกว่า 56 พิกเซล เพื่อให้กดง่ายบนหน้าจอสัมผัส ผู้เรียนเปลี่ยนคำตอบได้จนกว่าครูจะปิดรอบนั้น

**สถานะส่งแล้ว** แสดงเครื่องหมายถูกและข้อความว่าส่งคำตอบแล้ว **ห้ามแสดงผลรวมของทั้งห้องในหน้าจอผู้เรียนโดยเด็ดขาด** เพราะจะทำให้ผู้ตอบทีหลังเห็นคำตอบของเพื่อนแล้วตอบตาม ซึ่งทำลายความหมายของการโหวตรอบแรก

**สถานะรอบที่ 2** แสดงข้อความว่าคุยกับเพื่อนแล้วตอบใหม่ พร้อมแสดงคำตอบเดิมของตนเองเป็นสีจาง เพื่อให้ผู้เรียนรู้ตัวว่ากำลังเปลี่ยนคำตอบหรือยืนยันคำตอบเดิม

### 5.2 หน้าจอครู ระหว่างสอน

แถบควบคุมที่ต้องกดได้ด้วยมือเดียวขณะยืนหน้าชั้น ประกอบด้วย

- ตัวนับผู้ตอบแบบสด เช่น ตอบแล้ว 27 จาก 32 คน พร้อมแถบความคืบหน้า
- ปุ่มเปิดรอบที่ 1 ปุ่มปิดรอบ ปุ่มเปิดรอบที่ 2 ปุ่มฉายผล
- ตัวจับเวลานับถอยหลังที่ตั้งค่าได้ ตรงกับเวลาในแผน
- ปุ่มข้ามไปคำถามถัดไปในชุดเดียวกัน

**คำแนะนำอัตโนมัติ** เมื่อปิดรอบที่ 1 ระบบแสดงข้อความช่วยตัดสินใจให้ครู โดยอิงหลักการของ Peer Instruction ว่าการถกเถียงกับเพื่อนได้ผลดีที่สุดเมื่อสัดส่วนผู้ตอบถูกรอบแรกอยู่ระหว่างร้อยละ 35 ถึง 70

| สัดส่วนตอบถูกรอบที่ 1 | ข้อความที่ระบบแนะนำ |
|---|---|
| ต่ำกว่าร้อยละ 35 | ผู้เรียนส่วนใหญ่ยังเข้าใจคลาดเคลื่อน ควรอธิบายเพิ่มเติมก่อนให้ถกเถียง |
| ร้อยละ 35 ถึง 70 | ช่วงที่เหมาะที่สุดสำหรับการถกเถียงกับเพื่อน ให้เปิดรอบที่ 2 ได้เลย |
| สูงกว่าร้อยละ 70 | ผู้เรียนส่วนใหญ่เข้าใจแล้ว อาจข้ามรอบที่ 2 แล้วไปคำถามถัดไปเพื่อประหยัดเวลา |

ฟังก์ชันนี้เป็นจุดที่ทำให้โมดูลนี้ต่างจากเครื่องมือสำรวจความเห็นทั่วไป เพราะช่วยครูตัดสินใจเชิงการสอนระหว่างคาบด้วยข้อมูล มิใช่เพียงแสดงผลโหวต

### 5.3 หน้าจอฉายผล สำหรับโปรเจคเตอร์

ต้องเปิดเป็นหน้าต่างแยกได้ เพื่อให้ครูฉายหน้านี้ขึ้นจอโดยที่หน้าจอควบคุมยังอยู่บนเครื่องของครู

- **โหมดรอบเดียว** กราฟแท่งแนวตั้งแสดงจำนวนผู้เลือกแต่ละตัวเลือก ตัวอักษรกำกับไม่เล็กกว่า 28 pt
- **โหมดเปรียบเทียบสองรอบ** กราฟแท่งคู่ แท่งสีเทาแทนรอบที่ 1 แท่งสีน้ำเงินแทนรอบที่ 2 พร้อมป้ายบอกจำนวนบนหัวแท่ง
- **การเฉลย** เมื่อครูกดปุ่มฉายผล ตัวเลือกที่ถูกต้องจึงเปลี่ยนเป็นสีเขียวพร้อมเครื่องหมายถูก **ก่อนกดปุ่มนี้ต้องไม่มีสัญญาณใดบ่งชี้ว่าตัวเลือกใดถูก** ทั้งสี ตำแหน่ง หรือลำดับ เพราะแผนกำหนดให้ครูยังไม่เฉลยในขั้น E2
- **แถบสรุปการเปลี่ยนคำตอบ** แสดงจำนวนผู้เรียนที่เปลี่ยนจากผิดเป็นถูก ซึ่งเป็นตัวเลขที่ครูใช้ชื่นชมชั้นเรียนได้ทันที
- **โหมดคลังคำ** แสดงคำที่ผู้เรียนพิมพ์ โดยคำที่ซ้ำกันมากมีขนาดใหญ่กว่า

### 5.4 ลำดับการทำงานในขั้น E2 ตามแผน

| นาทีที่ | ครูทำ | ผู้เรียนเห็น | ระบบทำ |
|---|---|---|---|
| 0.0 | กดเปิดรอบที่ 1 ของคำถามที่ 1 | โจทย์และปุ่มตัวเลือก | สถานะเปลี่ยนเป็น round1_open ทุกเครื่องอัปเดตภายใน 2 วินาที |
| 0.0–2.5 | ดูตัวนับผู้ตอบ | ตอบและเห็นเครื่องหมายถูก | เขียนฟิลด์ r1 ของแต่ละคน |
| 2.5 | กดปิดรอบ | หน้าจอล็อก | คำนวณสัดส่วนตอบถูกและแสดงคำแนะนำแก่ครู |
| 2.5–6.0 | ประกาศให้ถกเถียงกับเพื่อน | ข้อความว่าคุยกับเพื่อนแล้วรอ | ไม่ทำงาน |
| 6.0 | กดเปิดรอบที่ 2 | โจทย์เดิมพร้อมคำตอบเดิมเป็นสีจาง | สถานะเปลี่ยนเป็น round2_open |
| 6.0–7.0 | ดูตัวนับ | ตอบใหม่ | เขียนฟิลด์ r2 และคำนวณ transition |
| 7.0 | กดฉายผล | — | สร้างกราฟเปรียบเทียบและเขียน pollAggregates |

---

## 6. การแสดงผลคลังคำและปัญหาการตัดคำภาษาไทย

การนับความถี่ของคำภาษาไทยยากกว่าภาษาอังกฤษ เพราะไม่มีช่องว่างคั่นระหว่างคำ มีสามทางเลือก

| วิธี | ข้อดี | ข้อเสีย | ข้อเสนอ |
|---|---|---|---|
| นับทั้งวลีที่ผู้เรียนพิมพ์ โดยตัดช่องว่างหัวท้ายและแปลงเป็นตัวพิมพ์เล็ก | ง่ายที่สุด ไม่ต้องพึ่งไลบรารี | คำว่า นับเงิน กับ การนับเงิน จะถูกนับแยกกัน | ใช้เป็นทางสำรอง |
| ใช้ `Intl.Segmenter` ของเบราว์เซอร์ ด้วย locale th และ granularity word | ตัดคำไทยได้ดีพอสมควร ไม่ต้องเพิ่มไลบรารี ทำงานในเบราว์เซอร์โดยตรง | รองรับใน Chrome ตั้งแต่รุ่น 87 ขึ้นไป ซึ่งเครื่องของผู้เรียนใช้ได้ | **เลือกวิธีนี้** พร้อมตรวจสอบว่ามีให้ใช้หรือไม่ แล้วถอยไปใช้วิธีแรกหากไม่มี |
| เพิ่มไลบรารีตัดคำภาษาไทย | แม่นยำที่สุด | เพิ่มขนาดไฟล์ที่ต้องดาวน์โหลด กระทบปริมาณข้อมูลของผู้เรียน | ไม่แนะนำสำหรับเวอร์ชันแรก |

**ข้อกำหนดเพิ่มเติม** ต้องมีรายการคำที่ไม่นับ เช่น ที่ การ ของ และ ใน แล้ว ครับ ค่ะ และครูต้องรวมคำที่มีความหมายเดียวกันด้วยมือได้ผ่านหน้าจอควบคุม เพราะภาษาที่ผู้เรียนพิมพ์มีความหลากหลายมาก

---

## 7. กฎความปลอดภัยของ Firestore

หลักการคือผู้เรียนเขียนได้เฉพาะคำตอบของตนเอง เขียนได้เฉพาะรอบที่เปิดอยู่ และอ่านคำตอบของผู้อื่นไม่ได้

```
match /pollSessions/{sessionId} {
  allow read: if request.auth != null;
  allow write: if isTeacher();

  match /responses/{uid} {
    // ผู้เรียนอ่านได้เฉพาะเอกสารของตนเอง ครูอ่านได้ทั้งหมด
    allow read: if request.auth.uid == uid || isTeacher();

    // เขียนได้เฉพาะเอกสารของตนเอง และเฉพาะรอบที่เปิดอยู่
    allow create, update: if request.auth.uid == uid
      && getSessionStatus(sessionId) in ['round1_open', 'round2_open']
      && writesOnlyActiveRound(sessionId);

    allow delete: if isTeacher();
  }
}
```

**ประเด็นสำคัญที่ต้องระวัง** ฟิลด์ `transition` และ `changed` ไม่ควรให้ผู้เรียนเขียนเอง เพราะเป็นข้อมูลที่ใช้ในการวิจัย ควรคำนวณด้วย Cloud Function หรือคำนวณฝั่งครูตอนปิดรอบแล้วเขียนทับ หากไม่ต้องการใช้ Cloud Function ให้คำนวณตอนส่งออกข้อมูลแทน ซึ่งเพียงพอสำหรับงานวิจัยในชั้นเรียน

---

## 8. การส่งออกข้อมูลเพื่อการวิจัย

### 8.1 ไฟล์รายบุคคล

```
sessionId, templateId, planId, stage, seq, classId, taughtOn, displayNo, r1_option, r1_correct,
r2_option, r2_correct, transition, r1_at, r2_at
```

**ต้องส่งออกเป็นเลขที่ในห้อง มิใช่ชื่อ** เพื่อให้สอดคล้องกับข้อกำหนดด้านจริยธรรมการวิจัยที่ระบุไว้ในเครื่องมือฉบับที่ 8 ว่าต้องรายงานโดยไม่ระบุตัวตน

### 8.2 ไฟล์สรุประดับชั้นเรียน

```
sessionId, templateId, prompt, classId, taughtOn, n, responded_r1, responded_r2,
correct_rate_r1, correct_rate_r2, normalized_gain,
wrong_to_right, right_to_right, right_to_wrong, wrong_to_wrong
```

### 8.3 ตัวชี้วัดที่ป้อนเข้าเครื่องมือวิจัยฉบับที่ 8

| ตัวชี้วัด | สูตร | ความหมายเชิงการเรียนรู้ |
|---|---|---|
| อัตราการเปลี่ยนจากผิดเป็นถูก | wrong_to_right หารด้วยจำนวนผู้ที่ตอบผิดในรอบที่ 1 | หลักฐานตรงว่าการถกเถียงกับเพื่อนทำให้เกิดการเรียนรู้ |
| อัตราการเปลี่ยนจากถูกเป็นผิด | right_to_wrong หารด้วยจำนวนผู้ที่ตอบถูกในรอบที่ 1 | สัญญาณเตือน หากสูงผิดปกติแสดงว่าตัวลวงมีปัญหาหรือผู้เรียนตามเพื่อนโดยไม่คิด |
| Normalized gain | (correct_r2 ลบ correct_r1) หารด้วย (1 ลบ correct_r1) | ขนาดของการเปลี่ยนแปลงเทียบกับพื้นที่ที่ยังพัฒนาได้ |
| สัดส่วนผู้ตอบครบ | responded หารด้วย n | หลักฐานความทั่วถึงของการมีส่วนร่วม สำหรับตัวชี้วัดที่ 1 และ 4 |

---

## 9. ข้อกำหนดที่มิใช่หน้าที่การทำงาน

| ด้าน | ข้อกำหนด |
|---|---|
| ความเร็ว | คำตอบของผู้เรียนต้องปรากฏบนตัวนับของครูภายใน 2 วินาที และการเปลี่ยนสถานะคำถามต้องถึงเครื่องผู้เรียนภายใน 2 วินาที |
| การใช้งานขณะสัญญาณขาด | เปิด offline persistence ของ Firestore คำตอบที่ส่งขณะสัญญาณขาดต้องถูกส่งอัตโนมัติเมื่อสัญญาณกลับมา |
| การเข้าร่วมสาย | ผู้เรียนที่เปิดหน้าจอหลังครูเปิดคำถามแล้ว ต้องเห็นคำถามทันทีโดยไม่ต้องรีเฟรช |
| การรีเฟรชหน้าจอ | คำตอบที่ส่งไปแล้วต้องยังคงอยู่ ไม่หายไป |
| ปริมาณข้อมูล | หน้าจอผู้เรียนต้องใช้ข้อมูลไม่เกิน 200 กิโลไบต์ต่อคำถาม |
| การมองเห็น | ห้ามใช้สีเพียงอย่างเดียวสื่อความถูกผิด ต้องมีเครื่องหมายหรือข้อความกำกับด้วย เพื่อรองรับผู้เรียนที่มีภาวะตาบอดสี |
| ความเป็นส่วนตัว | หน้าจอที่ฉายขึ้นโปรเจคเตอร์ต้องไม่แสดงชื่อหรือเลขที่ของผู้เรียนในทุกกรณี |

---

## 10. แผนการพัฒนาเป็นระยะ

| ระยะ | ขอบเขต | ผลลัพธ์ที่ใช้งานได้ |
|---|---|---|
| ระยะที่ 1 | คำถามเลือกตอบรอบเดียว หน้าจอผู้เรียน หน้าจอควบคุม กราฟแท่ง | ใช้แทนการยกมือได้ทันที ทดลองใช้ในคาบจริงได้ |
| ระยะที่ 2 | เพิ่มรอบที่ 2 กราฟเปรียบเทียบ การคำนวณ transition และคำแนะนำอัตโนมัติแก่ครู | ใช้กับขั้น E2 ของแผนที่ 19 ได้ครบตามที่ออกแบบ |
| ระยะที่ 3 | คำถามปลายเปิดและคลังคำ | ใช้กับขั้น E1 ได้ ครบทั้งแผน |
| ระยะที่ 4 | ส่งออก CSV และเชื่อมกับแผงวิเคราะห์เดิมของ APCC | ได้ข้อมูลเชิงประจักษ์สำหรับรายงานวิจัย |
| ระยะที่ 5 | คลังคำถามที่ผูกกับแผนทั้ง 37 แผน และการนำกลับมาใช้ซ้ำ | ขยายผลไปทุกแผน และให้ครูผู้อื่นนำไปใช้ได้ |

**ข้อเสนอ** ระยะที่ 1 และ 2 เพียงพอต่อการใช้ในแผนที่ 19 แล้ว ควรทำสองระยะนี้ให้เสร็จและทดลองใช้จริงก่อนหนึ่งคาบ แล้วจึงพัฒนาระยะที่ 3 ต่อ เพราะคลังคำมีความซับซ้อนเรื่องการตัดคำภาษาไทยซึ่งอาจใช้เวลานานกว่าที่คาด

---

## 11. เกณฑ์การยอมรับ (Acceptance Criteria)

ทดสอบด้วยผู้เรียนจริง 32 คนในห้องปฏิบัติการ ก่อนนำไปใช้ในคาบที่จะบันทึกวีดิทัศน์

- [ ] ครูเปิดคำถามแล้ว ผู้เรียนทั้ง 32 เครื่องเห็นคำถามภายใน 2 วินาที
- [ ] ผู้เรียนกดตอบแล้ว ตัวนับของครูเพิ่มขึ้นภายใน 2 วินาที
- [ ] ผู้เรียนเปลี่ยนคำตอบได้ก่อนครูปิดรอบ และเปลี่ยนไม่ได้หลังปิดรอบ
- [ ] ผู้เรียนที่เข้าสายเห็นคำถามทันทีโดยไม่ต้องรีเฟรช
- [ ] ผู้เรียนที่รีเฟรชหน้าจอยังเห็นคำตอบเดิมของตน
- [ ] หน้าจอผู้เรียนไม่แสดงผลรวมของทั้งห้องในทุกสถานะ
- [ ] หน้าจอฉายผลไม่บ่งชี้คำตอบที่ถูกก่อนที่ครูจะกดปุ่มฉายผล
- [ ] กราฟเปรียบเทียบสองรอบแสดงถูกต้อง และจำนวนรวมตรงกับจำนวนผู้ตอบจริง
- [ ] ปิดสัญญาณอินเทอร์เน็ตของเครื่องหนึ่งขณะตอบ แล้วเปิดใหม่ คำตอบต้องถูกส่งสำเร็จ
- [ ] ไฟล์ CSV ที่ส่งออกมีเลขที่ในห้องเท่านั้น ไม่มีชื่อผู้เรียน
- [ ] ค่า transition ที่คำนวณได้ตรงกับการนับด้วยมือจากข้อมูลตัวอย่าง 10 ราย
- [ ] หน้าจอฉายผลอ่านออกจากระยะ 8 เมตรในห้องที่หรี่ไฟ

---

## 12. ความเสี่ยงและแนวทางรับมือ

| ความเสี่ยง | ผลกระทบ | แนวทางรับมือ |
|---|---|---|
| อินเทอร์เน็ตของห้องล่มระหว่างบันทึกวีดิทัศน์ | กิจกรรมทั้งขั้น E2 ทำไม่ได้ | เตรียมบัตรคำตอบ ก ข ค ง แบบกระดาษไว้เป็นแผนสำรองเสมอ และซ้อมใช้ทั้งสองแบบก่อนวันบันทึกจริง |
| ผู้เรียนตอบแทนกันหรือเปิดหลายบัญชี | ข้อมูลวิจัยคลาดเคลื่อน | ผูกคำตอบกับรหัสผู้ใช้ที่ล็อกอินอยู่ และตรวจสอบจำนวนผู้ตอบเทียบกับจำนวนผู้เข้าเรียนจริง |
| เวลาบานเกินที่กำหนดในแผน | กระทบขั้น E4 ซึ่งเป็นขั้นหลัก | ตั้งตัวจับเวลานับถอยหลังบนหน้าจอฉายผล และให้ครูซ้อมกดปุ่มควบคุมจนคล่องก่อนสอนจริง |
| การตัดคำภาษาไทยไม่แม่นยำ ทำให้คลังคำดูรก | เสียเวลาในขั้น E1 | ให้ครูรวมคำที่มีความหมายเดียวกันด้วยมือได้ และจำกัดจำนวนคำที่แสดงไม่เกิน 20 คำ |
| ใช้โควตาฟรีของ Firestore หมด | ระบบหยุดทำงานกลางคาบ | ปริมาณที่ใช้จริงต่ำมาก คือประมาณ 500 การอ่านและ 250 การเขียนต่อคาบ ซึ่งอยู่ในโควตาฟรี 50,000 การอ่านต่อวันอย่างสบาย แต่ควรตั้งการแจ้งเตือนโควตาไว้ |

---

## 13. ประเด็นที่ต้องตัดสินใจก่อนเริ่มพัฒนา

1. จะใช้ Cloud Function คำนวณ transition หรือคำนวณตอนส่งออกข้อมูล ซึ่งวิธีหลังง่ายกว่าและไม่มีค่าใช้จ่าย แต่ข้อมูลจะไม่พร้อมทันทีหลังปิดรอบ
2. หน้าจอฉายผลจะเปิดเป็นหน้าต่างแยกด้วย `window.open` หรือใช้เส้นทาง URL แยกที่ครูเปิดเองในแท็บที่สอง
3. คำถามจะสร้างล่วงหน้าไว้ในระบบทั้งชุด หรือให้ครูพิมพ์สดหน้าชั้นได้ด้วย ซึ่งอย่างแรกปลอดภัยกว่าสำหรับคาบที่บันทึกวีดิทัศน์
4. จะเก็บข้อมูลย้อนหลังกี่ภาคเรียน และมีนโยบายลบข้อมูลผู้เรียนที่จบการศึกษาแล้วอย่างไร ซึ่งควรระบุไว้ในหัวข้อจริยธรรมการวิจัยของรายงานด้วย

---

## 14. สิ่งที่ต้องเตรียมก่อนส่งสเปกนี้ให้ Claude Code

สเปกฉบับนี้อธิบายว่าต้องสร้างอะไร แต่ยังไม่รู้จักโครงสร้างโค้ดเดิมของ APCC ก่อนเริ่มพัฒนาจึงต้องเตรียมข้อมูลต่อไปนี้ให้ครบ มิฉะนั้นจะได้โค้ดที่ไม่เข้ากับระบบเดิมและต้องแก้ซ้ำ

### 14.1 ข้อมูลจากโค้ดเดิมที่ต้องบอก

| สิ่งที่ต้องบอก | เหตุผล |
|---|---|
| ไฟล์ตั้งค่า Firebase อยู่ที่ใด และส่งออกอ็อบเจกต์ชื่ออะไร | โมดูลใหม่ต้องใช้อินสแตนซ์เดิม ห้ามสร้างการเชื่อมต่อซ้ำ |
| ระบบยืนยันตัวตนปัจจุบันเก็บบทบาทผู้ใช้อย่างไร | กฎความปลอดภัยใช้ฟังก์ชัน isTeacher ซึ่งต้องเขียนให้ตรงกับวิธีที่ระบบเดิมแยกครูกับผู้เรียน |
| เก็บเลขที่ในห้องของผู้เรียนไว้ที่ฟิลด์ใด | ใช้เป็นค่า displayNo ในการส่งออกข้อมูลวิจัยโดยไม่ระบุชื่อ |
| ใช้ระบบกำหนดเส้นทางแบบใด และไฟล์เส้นทางอยู่ที่ใด | ต้องเพิ่มเส้นทางใหม่ให้เข้ากับรูปแบบเดิม |
| โครงสร้างโฟลเดอร์ของคอมโพเนนต์เป็นอย่างไร | วางไฟล์ใหม่ให้ตรงกับแบบแผนเดิม |
| ใช้ Chart.js เวอร์ชันใด และห่อหุ้มเป็นคอมโพเนนต์ไว้แล้วหรือไม่ | นำมาใช้ซ้ำแทนการเขียนกราฟใหม่ |
| กฎความปลอดภัยของ Firestore ปัจจุบันอยู่ในไฟล์ใด | ต้องเพิ่มกฎใหม่ต่อท้ายโดยไม่กระทบของเดิม |

วิธีที่เร็วที่สุดคือให้ Claude Code อ่านโปรเจกต์แล้วสรุปให้ก่อน ด้วยคำสั่งว่า ช่วยอ่านโครงสร้างโปรเจกต์นี้แล้วสรุปเจ็ดข้อข้างต้นให้หน่อย

### 14.2 คำถามสี่ข้อในหัวข้อ 13 ที่ต้องตัดสินใจก่อน

แนะนำคำตอบสำหรับเวอร์ชันแรก เพื่อให้เริ่มได้เร็วที่สุด

1. **การคำนวณ transition** ให้คำนวณฝั่งไคลเอนต์ตอนครูกดปิดรอบที่ 2 แล้วเขียนกลับลงเอกสาร ไม่ต้องใช้ Cloud Function เพราะจำนวนผู้เรียนน้อยและไม่มีค่าใช้จ่ายเพิ่ม
2. **หน้าจอฉายผล** ให้ใช้เส้นทาง URL แยก เช่น /poll/present/:sessionId แล้วครูเปิดในแท็บที่สองลากไปจอโปรเจคเตอร์ วิธีนี้ควบคุมง่ายกว่าและไม่ติดปัญหาการปิดกั้นหน้าต่างป๊อปอัป
3. **การสร้างคำถาม** ให้สร้างล่วงหน้าไว้ในคลังทั้งชุด ไม่ต้องพิมพ์สดหน้าชั้น เพราะคาบที่บันทึกวีดิทัศน์ต้องไม่มีความเสี่ยง
4. **การเก็บข้อมูลย้อนหลัง** เก็บสามปีการศึกษา แล้วลบเอกสาร responses ของรุ่นที่จบแล้ว โดยคง pollAggregates ไว้เพราะไม่มีข้อมูลรายบุคคล

### 14.3 คำสั่งเริ่มงานสำหรับ Claude Code

ใช้ข้อความต่อไปนี้เป็นคำสั่งแรกหลังเปิดโปรเจกต์ APCC ใน VS Code

```
ในโปรเจกต์นี้มีไฟล์สเปก Spec_QuickPoll_APCC.md อยู่ ให้อ่านทั้งไฟล์ก่อน

จากนั้นทำสามอย่างตามลำดับ อย่าเพิ่งเขียนโค้ด

หนึ่ง อ่านโครงสร้างโปรเจกต์แล้วสรุปให้ทราบว่า ไฟล์ตั้งค่า Firebase อยู่ที่ใด
ระบบยืนยันตัวตนแยกบทบาทครูกับผู้เรียนอย่างไร เก็บเลขที่ในห้องไว้ที่ฟิลด์ใด
ใช้ระบบกำหนดเส้นทางแบบใด โครงสร้างโฟลเดอร์คอมโพเนนต์เป็นอย่างไร
ใช้ Chart.js อย่างไร และไฟล์กฎความปลอดภัยของ Firestore อยู่ที่ใด

สอง เสนอรายการไฟล์ที่จะสร้างใหม่และไฟล์เดิมที่ต้องแก้ พร้อมเหตุผลสั้น ๆ
ต่อไฟล์ โดยยึดแบบแผนการตั้งชื่อและโครงสร้างโฟลเดอร์ที่โปรเจกต์นี้ใช้อยู่

สาม ระบุจุดที่สเปกยังไม่ชัดเจนหรือขัดกับโค้ดเดิม แล้วถามกลับมา
อย่าเดาเอง

เมื่อผมยืนยันแผนแล้วจึงเริ่มพัฒนาระยะที่ 1 เท่านั้น คือคำถามเลือกตอบ
รอบเดียว หน้าจอผู้เรียน หน้าจอควบคุมของครู และกราฟแท่ง
ยังไม่ต้องทำรอบที่สอง คลังคำ หรือการส่งออกข้อมูล
```

**เหตุผลที่ต้องสั่งไม่ให้เขียนโค้ดทันที** สเปกนี้ละเอียดพอที่ระบบจะเริ่มเขียนได้เลย แต่จะเขียนโดยสมมติโครงสร้างโค้ดเดิมเอาเอง ซึ่งมักไม่ตรงกับของจริง ทำให้ต้องรื้อทั้งหมด การให้สำรวจและเสนอแผนก่อนใช้เวลาเพิ่มเพียงไม่กี่นาที แต่ประหยัดเวลาแก้ไขได้มาก

### 14.4 ลำดับการสั่งงานที่แนะนำ

| ครั้งที่ | สิ่งที่สั่ง | เกณฑ์ที่ถือว่าผ่าน |
|---|---|---|
| 1 | สำรวจโปรเจกต์และเสนอแผนไฟล์ | แผนไฟล์สอดคล้องกับโครงสร้างเดิม และมีคำถามกลับมาอย่างน้อยหนึ่งข้อ |
| 2 | สร้างแบบจำลองข้อมูลและกฎความปลอดภัย พร้อมสคริปต์เพิ่มคำถามตัวอย่าง 3 ข้อของแผนที่ 19 | เพิ่มข้อมูลลง Firestore ได้จริง และกฎปฏิเสธการเขียนข้ามผู้ใช้ |
| 3 | สร้างหน้าจอผู้เรียน | เปิดสองแท็บด้วยบัญชีต่างกัน ตอบแล้วข้อมูลเข้า Firestore ถูกเอกสาร |
| 4 | สร้างหน้าจอควบคุมของครู | ตัวนับเพิ่มขึ้นภายใน 2 วินาทีเมื่อผู้เรียนตอบ |
| 5 | สร้างหน้าจอฉายผลและกราฟแท่ง | ตัวอักษรอ่านออกจากระยะ 8 เมตร และไม่บ่งชี้คำตอบถูกก่อนกดปุ่มเฉลย |
| 6 | ทดสอบตามเกณฑ์การยอมรับในหัวข้อ 11 | ผ่านทุกข้อของระยะที่ 1 |

อย่าสั่งหลายอย่างพร้อมกันในครั้งเดียว เพราะจะตรวจสอบได้ยากว่าส่วนใดทำงานผิด และการแก้ไขจะกระทบหลายไฟล์พร้อมกัน
